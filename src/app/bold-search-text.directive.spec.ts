import { BoldSearchTextDirective } from './bold-search-text.directive';

describe('BoldSearchTextDirective', () => {
  it('should create an instance', () => {
    const directive = new BoldSearchTextDirective();
    expect(directive).toBeTruthy();
  });
});
